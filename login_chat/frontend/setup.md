# Setup

```powershell
cd login_chat/frontend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run app.py
```

`http://127.0.0.1:8000`에서 `login_chat/backend`이 먼저 실행 중이어야 합니다.
