# Setup

```powershell
cd frontend_login_chat
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run app.py
```

`http://127.0.0.1:8000`에서 `backend_login_chat`이 먼저 실행 중이어야 합니다.
