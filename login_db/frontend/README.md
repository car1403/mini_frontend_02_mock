# Supabase DB 로그인 Frontend

`login_db/backend`의 회원가입과 로그인 API를 사용하는 Streamlit 화면입니다.

```powershell
cd login_db\frontend
pip install -r requirements.txt
streamlit run app.py
```

회원가입 화면에서 계정을 만든 뒤 같은 계정으로 로그인합니다.
